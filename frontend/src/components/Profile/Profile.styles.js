import styled from 'styled-components';

export const Profile = styled.section`
  position: absolute;
  top: 1rem;
  right: 1rem;
  background: #efd;
  box-shadow: 0 0 5px;
  border-radius: 3%;
  padding: 1rem 2rem;
`;
